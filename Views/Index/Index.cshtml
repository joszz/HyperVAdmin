@using HyperVAdmin.Models
@using Microsoft.Web.Administration

@model List<VirtualMachine>

@{
    ViewBag.Title = "Hyper-V Admin";
}

<div class="panel panel-primary">
    <div class="panel-heading">
        <div class="pull-right btn-group">
            <button class="btn btn-default">
                <i class="glyphicon glyphicon-refresh"></i>
            </button>
        </div>
        <h4>Virtual machines</h4>
    </div>
    <div class="panel-body table-responsive">
        <table class="table table-hover">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Last state change</th>
                    <th>On time</th>
                    <th class="text-center">Cores</th>
                    <th>Memory</th>
                    <th>MAC</th>
                    <th class="text-right">Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (VirtualMachine vm in Model)
                {
                    <tr>
                        <td>@vm.Name</td>
                        <td>@vm.TimeOfLastStateChangeFormatted</td>
                        <td>@vm.GetOnTimeFormatted</td>
                        <td class="text-center">@vm.CoresAmount</td>
                        <td>@vm.MemoryTotal @vm.MemoryAllocationUnits</td>
                        <td>@vm.MAC</td>
                        <td class="text-right">
                            <div class="btn-group">
                                <button class="btn btn-@Html.Raw(vm.State == VirtualMachineState.Enabled ? "success" : "danger") glyphicon glyphicon-off" />
                            </div>
                        </td>
                    </tr>
                }

                <tr class="hidden cloneable">
                    <td></td>
                    <td></td>
                    <td></td>
                    <td class="text-center"></td>
                    <td></td>
                    <td></td>
                    <td class="text-right">
                        <div class="btn-group">
                            <button class="btn glyphicon glyphicon-off" />
                        </div>
                    </td>
                </tr>
            </tbody>
        </table>
    </div>
</div>